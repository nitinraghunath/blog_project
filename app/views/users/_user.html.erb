<li>
  <%= gravatar_for user%>
  <%= link_to user.name %>
  <% if current_user.try(:admin?) %>
    | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>